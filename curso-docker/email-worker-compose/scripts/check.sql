/* Lista todos os bancos de dados cadastrados. */
\l

/* Conecta ao banco email_sender */
\c email_sender

/* Gera uma descrição da tabela emails, mostrando as colunas para verificar que de fato o script de inicialização foi executado de maneira bem sucedida. */
\d emails